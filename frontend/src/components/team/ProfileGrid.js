import * as React from "react"

const ProfileGrid = ({ displayLimit }) => {
    return (
        <div class="grid-wrapper">
            <div class="grid">

            </div>
        </div>
    )
}

export default ProfileGrid